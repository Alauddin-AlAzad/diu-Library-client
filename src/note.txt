import React, { useState } from 'react';
import { MdOutlineAddBox } from "react-icons/md";

const Addbook = () => {
    const [imagePreview, setImagePreview] = useState(null);

    const handleImageChange = (e) => {
        const file = e.target.files[0];
        if (file) {
            setImagePreview(URL.createObjectURL(file));
        } else {
            setImagePreview(null);
        }
    };

    return (
        <div className='container mx-auto px-4 my-5 lg:my-8'>

            {/* Header */}
            <div className='flex flex-col md:flex-row items-start md:items-center justify-between mb-5 gap-3'>
                <div className='flex items-center gap-2 font-semibold text-lg lg:text-2xl text-gray-800'>
                    <MdOutlineAddBox className='text-xl lg:text-2xl text-blue-600' />
                    <p>Add New Books</p>
                </div>

                <div className='flex gap-2 flex-wrap'>
                    <button className='text-xs border border-gray-300 px-3 py-1 rounded hover:bg-gray-100 transition'>
                        Save As Draft
                    </button>
                    <button className='text-xs bg-blue-600 hover:bg-blue-700 text-white px-3 py-1 rounded transition'>
                        Add Books
                    </button>
                </div>
            </div>

            {/* Section 1 - Flex layout for responsive side-by-side */}
            <div className='flex flex-col md:flex-row gap-4 my-5 bg-[#F7F9FC] p-4 rounded-xl'>

                {/* Left Content */}
                <div className='flex-1 border border-gray-200 rounded-xl p-4 shadow-sm bg-white'>
                    <h2 className='text-lg font-semibold mb-3'>General Information</h2>

                    <label htmlFor="bookName" className='text-sm text-gray-600 mb-1 block'>Book Name</label>
                    <input id="bookName" type="text" placeholder="Enter book name" className="input w-full" />

                    <label htmlFor="authorName" className='text-sm text-gray-600 mt-4 mb-1 block'>Author Name</label>
                    <input id="authorName" type="text" placeholder="Enter author name" className="input w-full" />

                    <label htmlFor="description" className='text-sm text-gray-600 mt-4 mb-1 block'>Short Description</label>
                    <textarea id="description" placeholder="Add more about this book" className="textarea w-full" />
                </div>

                {/* Right Content */}
                <div className='w-full md:w-1/3 border border-gray-200 rounded-xl p-4 shadow-sm bg-white flex flex-col items-center'>
                    <h2 className='text-sm md:text-base mb-2 text-gray-600 font-medium'>Upload Book Image</h2>

                    {/* Thumbnail Preview */}
                    {imagePreview && (
                        <div className='mb-3'>
                            <img 
                                src={imagePreview} 
                                alt="Thumbnail Preview" 
                                className='w-24 h-36 object-cover rounded-md border'
                            />
                        </div>
                    )}

                    <input 
                        required
                        id='image'
                        name='image'
                        accept='image/*'
                        type="file"
                        onChange={handleImageChange}
                        className="w-full text-sm md:text-[16px] text-[12px]" 
                    />
                </div>
            </div>

            {/* Section 2 */}
            <div className='grid grid-cols-1 lg:grid-cols-8 gap-4 bg-[#F7F9FC] p-4 rounded-xl'>

                {/* Left */}
                <div className='col-span-5 border border-gray-200 rounded-xl p-4 shadow-sm bg-white'>
                    <h2 className='text-lg font-semibold mb-3'>Rating And Quantity</h2>

                    <div className='grid grid-cols-1 md:grid-cols-2 gap-4'>
                        <div>
                            <label htmlFor="quantity" className='text-sm text-gray-600 mb-1 block'>Quantity</label>
                            <input id="quantity" type="number" min='1' placeholder="Enter quantity" className="input w-full" />
                        </div>

                        <div>
                            <label htmlFor="rating" className='text-sm text-gray-600 mb-1 block'>Rating</label>
                            <input id="rating" type="number" min="1" max="5" placeholder="1-5" className="input w-full" />
                        </div>
                    </div>

                    <label htmlFor="content" className='text-sm text-gray-600 mt-4 mb-1 block'>Book Content</label>
                    <textarea id="content" placeholder="Write book content..." className="textarea w-full" />
                </div>

                {/* Right */}
                <div className='col-span-3 border border-gray-200 rounded-xl p-4 shadow-sm bg-white'>
                    <h2 className='text-lg font-semibold mb-3'>Category</h2>

                    <label htmlFor="category" className='text-sm text-gray-600 mb-1 block'>Select Category</label>
                    <select id="category" className="input w-full">
                        <option>Select category</option>
                        <option>Novel</option>
                        <option>Thriller</option>
                        <option>History</option>
                        <option>Science</option>
                    </select>

                    <button className='mt-4 w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded text-sm'>
                        Add Category
                    </button>
                </div>
            </div>
        </div>
    );
};

export default Addbook;